import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  Image: {
    resizeMode: "contain",
    height: 190,
    width: "100%",
    marginTop: 20,
  },
  content: {
    marginHorizontal: 40,
  },
});
